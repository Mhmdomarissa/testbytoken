# The captured connector is committed as evidence: it must keep its original
# LF-only line endings, which are the reason cmd.exe cannot run it.
"reference/UTS-Connect-JackTest.bat" -text

# A Windows .bat must be CRLF or cmd.exe mis-parses it. Pin it on checkout.
"reference/UTS-Connect-JackTest-FIXED.bat" text eol=crlf
